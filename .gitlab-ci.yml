stages:
  - erstellung-csv
  - artifact-to-git

erstellung-csv:
  stage: erstellung-csv
  image: rocker/tidyverse:4.0.2
  script:
    - Rscript rfl-war.R
  artifacts:
    paths:
      - output.csv
  only:
    - main

artifact-to-git:
  stage: artifact-to-git
  image: docker:latest
  script:
    - bash artifact-to-git.sh
  only:
    - main
